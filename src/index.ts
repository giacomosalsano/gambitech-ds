/**
 * Public entry point for @gambitech/ds.
 *
 * Components are intentionally not exported yet: the repository is in the
 * foundation/tooling phase (see docs/ai/EPICS.md — Epic 1). Primitives will be
 * added here as they are implemented, using explicit named exports only.
 */

export { cn } from "./lib/utils";
