---
"@gambitech/ds": minor
---

Add the `Textarea` form primitive (Epic 2 Inputs).

- `Textarea` — token-only native `<textarea>` sharing the `Input` visual
  language (border, `focus-visible`, `aria-invalid`, `disabled`), with
  `field-sizing-content` for content-based auto-grow. Ships a matching
  `TextareaSkeleton` (ADR-0005) mirroring the default `min-h-16` footprint.

Exported from `@gambitech/ds`. No hardcoded colors; light/dark ready.
