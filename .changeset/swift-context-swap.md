---
"@gambitech/ds": minor
---

Add `ContextSwitcher` composite (Epic 4).

- Multi-membership toggle on `DropdownMenu` + radio items (`Trigger`/
  `TriggerLabel`/`TriggerValue`/`Content`/`Label`/`Items`/`Item`).
- Controlled/uncontrolled `value`/`onValueChange`; i18n via children
  (ADR-0002). Ships `ContextSwitcherSkeleton` (ADR-0005).

Exported from `@gambitech/ds`. No hardcoded colors; light/dark ready.
