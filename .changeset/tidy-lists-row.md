---
"@gambitech/ds": minor
---

Add `DataList` / `EntityRow` composites (Epic 4).

- `DataList`: bordered divided list surface.
- `EntityRow`: `Content`/`Title`/`Meta`/`Value`/`Actions` (name + meta +
  price + actions). Skeletons included (ADR-0005). Closes Epic 4.

Exported from `@gambitech/ds`. No hardcoded colors; light/dark ready.
