---
"@gambitech/ds": minor
---

Add `Command` primitive (Epic 3).

- Wraps `cmdk` with token-only styling (`Input`/`List`/`Empty`/`Group`/`Item`/
  `Separator`/`Shortcut`).
- Ships `CommandDialog` (palette over `Dialog`) and `CommandSkeleton`
  (ADR-0005). Combobox pattern demonstrated via Popover composition in
  Storybook.

Exported from `@gambitech/ds`. No hardcoded colors; light/dark ready.
