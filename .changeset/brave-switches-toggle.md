---
"@gambitech/ds": minor
---

Add the `Switch` form primitive, completing the Epic 2 Inputs set.

- `Switch` — wraps `@radix-ui/react-switch`; token-only with `checked`,
  `disabled` and `aria-invalid` states and a sliding thumb. Ships a matching
  `SwitchSkeleton` (ADR-0005).

Exported from `@gambitech/ds`. No hardcoded colors; light/dark ready.
