---
"@gambitech/ds": minor
---

Add `MetricCard` composite (Epic 4).

- Compound metric surface (`Label`/`Value`/`Empty`) on card tokens.
- Empty copy via children for i18n; ships `MetricCardSkeleton` (ADR-0005).

Exported from `@gambitech/ds`. No hardcoded colors; light/dark ready.
